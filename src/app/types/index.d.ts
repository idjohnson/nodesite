export interface postMetadata {
	title: string;
	subtitle: string;
	date: string;
	slug: string;
	tags: string[];
}

export interface portfolioMetadata {
	title: string;
	subtitle: string;
	date: sting;
	slug: string;
	previewImg: string;
}

export interface icon {
	size?: number;
}
